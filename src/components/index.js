import React from 'react';

 const bodyComponent = ()=>(<div>This is template for staring a project with login , logout feature. And also managed project structure.</div>);

 export default bodyComponent;